{"version":"0.2.0","body":"\nfunction main(workbook: ExcelScript.Workbook) {\n  // Add a new worksheet with name \"POsDraft\"\n  let POsDraft = workbook.addWorksheet(\"POs_Draft\");\n\n  // Auto fill range\n  //POsDraft.getRange(\"A1\").autoFill(\"A1:E1\", ExcelScript.AutoFillType.fillDefault);\n  POsDraft.getRange(\"A1\").autoFill(\"A1:F1\", ExcelScript.AutoFillType.fillDefault);\n  let selectedSheet = workbook.getActiveWorksheet();\n  // Paste to range A:A on POsDraft from range B:B on selectedSheet\n  POsDraft.getRange(\"A:A\").copyFrom(selectedSheet.getRange(\"B:B\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range B:B on POsDraft from range U:U on selectedSheet\n  POsDraft.getRange(\"B:B\").copyFrom(selectedSheet.getRange(\"U:U\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range C:C on POsDraft from range G:G on selectedSheet\n  POsDraft.getRange(\"C:C\").copyFrom(selectedSheet.getRange(\"G:G\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range D:D on POsDraft from range X:X on selectedSheet\n  POsDraft.getRange(\"D:D\").copyFrom(selectedSheet.getRange(\"X:X\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range E:E on POsDraft from range AC:AC on selectedSheet\n  POsDraft.getRange(\"E:E\").copyFrom(selectedSheet.getRange(\"AC:AC\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range F:F on pOs from range Y:Y on selectedSheet\n  POsDraft.getRange(\"F:F\").copyFrom(selectedSheet.getRange(\"AQ:AQ\"), ExcelScript.RangeCopyType.all, false, false);\n  // Remove duplicates from range A1:E1838 on POsDraft\n  POsDraft.getUsedRange().removeDuplicates([0], true);\n\n  let POs = workbook.getWorksheet(\"POs_Draft\");\n  let usedRange = POs.getUsedRange()\n  // Create a new worksheet with a default name.\n  let newSheet = workbook.addWorksheet(\"POs\");\n  // Copy the values from the used range to the new worksheet.\n  let copyType = ExcelScript.RangeCopyType.values; /* Change this to copy different information, such as formats. */\n  let targetRange = newSheet.getRangeByIndexes(\n    usedRange.getRowIndex(),\n    usedRange.getColumnIndex(),\n    usedRange.getRowCount(),\n    usedRange.getColumnCount());\n  targetRange.copyFrom(usedRange, copyType);\n  let newTable = workbook.addTable(targetRange, true);\n  newTable.setName(\"POTable\");\n\n  // Add a new worksheet with name \"PO Items\"\n  let PO_items_Draft = workbook.addWorksheet(\"PO Items Draft\");\n  // Auto fill range\n  PO_items_Draft.getRange(\"A1\").autoFill(\"A1:K1\", ExcelScript.AutoFillType.fillDefault);\n  // Paste to range A:A on PO_items_Draft from range B:B on selectedSheet\n  PO_items_Draft.getRange(\"A:A\").copyFrom(selectedSheet.getRange(\"B:B\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range B:B on PO_items_Draft from range C:C on selectedSheet\n  PO_items_Draft.getRange(\"B:B\").copyFrom(selectedSheet.getRange(\"C:C\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range C:C on PO_items_Draft from range M:M on selectedSheet\n  PO_items_Draft.getRange(\"C:C\").copyFrom(selectedSheet.getRange(\"M:M\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range D:D on PO_items_Draft from range L:L on selectedSheet\n  PO_items_Draft.getRange(\"D:D\").copyFrom(selectedSheet.getRange(\"L:L\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range E:E on PO_items_Draft from range S:S on selectedSheet\n  PO_items_Draft.getRange(\"E:E\").copyFrom(selectedSheet.getRange(\"S:S\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range F:F on PO_items_Draft from range T:T on selectedSheet\n  PO_items_Draft.getRange(\"F:F\").copyFrom(selectedSheet.getRange(\"T:T\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range G:G on PO_items_Draft from range AC:AC on selectedSheet\n  PO_items_Draft.getRange(\"G:G\").copyFrom(selectedSheet.getRange(\"AC:AC\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range H:H on PO_items_Draft from range AE:AE on selectedSheet\n  PO_items_Draft.getRange(\"H:H\").copyFrom(selectedSheet.getRange(\"AE:AE\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range I:I on PO_items_Draft from range AG:AG on selectedSheet\n  PO_items_Draft.getRange(\"I:I\").copyFrom(selectedSheet.getRange(\"AG:AG\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range J:J on PO_items_Draft from range AL:AL on selectedSheet\n  PO_items_Draft.getRange(\"J:J\").copyFrom(selectedSheet.getRange(\"AL:AL\"), ExcelScript.RangeCopyType.all, false, false);\n  // Paste to range K:K on PO_items_Draft from range D:D on selectedSheet\n  PO_items_Draft.getRange(\"K:K\").copyFrom(selectedSheet.getRange(\"D:D\"), ExcelScript.RangeCopyType.all, false, false);\n  // Auto fill range\n  PO_items_Draft.getRange(\"K1\").autoFill(\"K1:L1\", ExcelScript.AutoFillType.fillDefault);\n  // Paste to range L:L on PO_items_Draft from range O:O on selectedSheet\n  PO_items_Draft.getRange(\"L:L\").copyFrom(selectedSheet.getRange(\"O:O\"), ExcelScript.RangeCopyType.all, false, false);\n\n\n  let PO_items = workbook.getWorksheet(\"PO Items Draft\");\n  let usedRange2 = PO_items.getUsedRange();\n  // Create a new worksheet with a default name.\n  let newSheet2 = workbook.addWorksheet(\"PO Items\");\n  // Copy the values from the used range to the new worksheet.\n  let copyType2 = ExcelScript.RangeCopyType.values; /* Change this to copy different information, such as formats. */\n  let targetRange2 = newSheet2.getRangeByIndexes(\n    usedRange2.getRowIndex(),\n    usedRange2.getColumnIndex(),\n    usedRange2.getRowCount(),\n    usedRange2.getColumnCount());\n  targetRange2.copyFrom(usedRange2, copyType2);\n  let newTable2 = workbook.addTable(targetRange2, true);\n  newTable.setName(\"PO_Items_Table\");\n  POsDraft.delete();\n  PO_items_Draft.delete();\n\n}\n\n\n","description":"","parameterInfo":"{\"originalParameterOrder\":[],\"parameterSchema\":{\"type\":\"object\",\"default\":{},\"x-ms-visibility\":\"internal\"},\"returnSchema\":{\"type\":\"object\",\"properties\":{}}}","apiInfo":"{\"variant\":\"synchronous\",\"variantVersion\":2}"}